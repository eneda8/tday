<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand font-weight-bold logo" href="/">todai</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/">home</a>
        <a class="nav-link" href="/posts/today">today</a>
        <% if(currentUser && (currentUser.postedToday === false)) {  %> 
        <a class="nav-link" href="" data-toggle="modal" id="newPostLink" data-target="#newPostModal">rate today</a>
        <% } %>
        <% if(currentUser && (currentUser.postedToday === true)) {  %> 
        <!-- <a class="nav-link" href="" data-toggle="modal" id="editPostLink" data-target="#editPostModal">Update Rating</a> -->
        <a href="/posts/<%=currentUser.todaysPost%>" class="nav-link">my rating</a>
        <% } %>
      </div>
      <div class="navbar-nav ml-auto mr-2">
          <form class="form-inline">
            <input class="form-control mr-sm-2" type="search" style = "height: 30px" placeholder="Search" aria-label="Search">
            <button class="btn btn-sm btn-outline-warning my-2 my-sm-0" type="submit">Search</button>
          </form>
          <% if(!currentUser) {  %> 
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/register">Register</a>
          <% } else { %> 
          <li class="nav-item dropdown mr-auto">
            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><img class="p-0" src="<%=currentUser.avatar.thumbnail%>" style= "border-radius: 50%;"alt=""></a>
            <div class="dropdown-menu" style="min-width: 5rem">
              <a class="dropdown-item p-2" href="/u/<%=currentUser.username%>">Profile</a>
              <a class="dropdown-item p-2" href="/settings">Settings</a>
              <a class="dropdown-item p-2" href="/logout">Logout</a>
            </div>
          </li>
          <% }  %>
      </div>
      </div>
    </div>
  </div>
</nav>

<%- include('../partials/new')%>
